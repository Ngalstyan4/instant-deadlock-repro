{
  "name": "instant-500",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "db:setup": "pnpm run db:schema && pnpm run db:perms",
    "db:schema": "npx instant-cli@latest push schema --yes",
    "db:perms": "npx instant-cli@latest push perms --yes",
    "test": "vitest --run",
    "test:watch": "vitest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.7.1",
  "dependencies": {
    "@instantdb/admin": "^0.18.7",
    "dotenv": "^16.5.0",
    "vitest": "^3.1.1"
  }
}
